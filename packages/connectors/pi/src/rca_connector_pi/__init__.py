"""rca_connector_pi — the PI connector (EPIC-013 S13.2).

Translates the PI Web API REST surface (the EPIC-002 PI simulator in dev, a real
PI server in prod) into the canonical MCP contract via connector_sdk. Product code:
never imports rca_simulator.
"""
